pipeline:
    build:
        image: python
        commands:
            #- pip3 install nose cython
            - apt update
            - apt install -y python3-nose cython3
            - nosetests3 --version
            - export PATH=$PATH:$HOME/.local/bin/
            - python3 setup.py develop --user
            - bash test.sh
